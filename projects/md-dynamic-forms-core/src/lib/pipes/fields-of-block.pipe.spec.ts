import { FieldsOfBlockPipe } from './fields-of-block.pipe';

describe('FieldsOfBlockPipe', () => {
  it('create an instance', () => {
    const pipe = new FieldsOfBlockPipe();
    expect(pipe).toBeTruthy();
  });
});
